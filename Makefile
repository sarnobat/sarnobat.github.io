new:
	echo "To create a new page, it needs the front matter"
	echo "See github_post_new.sh"

build:
	echo "To see your changes, commit and push"
	echo "(it will take a few seconds to run the jekyll static generator workflow)"
	
view:
	echo "To see your changes: https://sarnobat.github.io/"